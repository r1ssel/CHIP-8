{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Executable",
            "type": "shell",
            "command": "${workspaceFolder}/build/chip8",
            "args": [
                "../pong2.c8"
            ],
            "dependsOn": "cmake build",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}